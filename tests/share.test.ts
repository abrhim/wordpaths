import { IDLE_TRANSITION } from "@remix-run/react/transition";
import { generateShareString } from "~/utils/share";

it("generates the proper string", () => {
  console.log("heyo");
  expects(true).toBe(true);
});
